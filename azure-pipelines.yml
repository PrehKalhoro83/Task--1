trigger:
- main

pool:
  name: Default

steps:

- task: Docker@2
  displayName: Build Docker Image
  inputs:
    command: build
    Dockerfile: '**/Dockerfile'
    repository: task1-app
    tags: |
      latest